# Topics to dive deeper into later

Things that came up during building that are worth exploring further once the current tasks are settled — parking these so they don't derail progress now.

- **REST conventions / API design** — why URL + method uniquely identifies one endpoint, how REST maps resources (URLs) to actions (HTTP methods: GET/POST/PUT/PATCH/DELETE), and how a real API grows beyond just GET/POST (e.g. `DELETE /api/users/:id` for removing a specific user, `PATCH /api/users/:id` for editing one).
